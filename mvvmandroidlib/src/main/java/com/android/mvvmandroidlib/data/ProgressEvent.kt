package com.android.mvvmandroidlib.data

/**
 * This enum class is used to define progress.
 * 1) Start Progress - it will start the progress
 * 2) Stop Progress - it will stop the progress
 *
 * @author Shubham Chauhan
 */
enum class ProgressEvent {
    START_PROGRESS,
    STOP_PROGRESS
}
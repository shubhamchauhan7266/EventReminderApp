package com.event.reminder.ui.dashboard

import com.event.reminder.data.repository.HomeRepository
import com.event.reminder.ui.BaseObservableViewModel

class HomeViewModel(private val homeRepository: HomeRepository) : BaseObservableViewModel(){

}
